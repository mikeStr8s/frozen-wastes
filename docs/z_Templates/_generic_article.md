<%*
const title = await tp.system.prompt("What is the Title?");
const descriptor = await tp.system.prompt("Quick descriptor. (i.e. species, role, location, type, etc.)");
%>
# <% title %>

*<% descriptor %>*